#!/usr/bin/env sh

REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null)"

if [ -z "$REPO_ROOT" ]; then
    echo "Unable to find repository root." >&2
    exit 1
fi

exec sh "$REPO_ROOT/tools/git-hooks/check-conventional-commit.sh" --message-file "$1"
