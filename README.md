# plinth

`plinth` is a C++ OpenGL rendering library to easily display points, lines and triangle meshes in a CAD like viewer.

See [`examples/example_renderer_standalone.cpp`](examples/example_renderer_standalone.cpp) for a complete minimal renderer loop. 
